services:
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-ferreyepes}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-ferreyepes}
      POSTGRES_DB: ${DB_NAME:-ferreyepes}
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
